name: publish
on:
  release:
    types:
      - published 
env:
  NPM_TOKEN: ${{ secrets.npm_token }}
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: echo releasing version 1.0 of test
      - uses: actions/checkout@v2
      - run: |
          rm -f .npmrc
          echo "//npm.pkg.github.com/:_authToken=\"$SERVICES_TOKEN\"" > ~/.npmrc
          echo ~/.npmrc
      - run: | 
          npm ci
          npm t
          npm publish --verbose


    
